package geometry;

/**
 * enum geometry.geometry.STATE.
 * this enum is used as an indicator for the state of the line,
 * if its a point or parallel to y axes or parallel to x axes or a normal line */
public enum STATE { normal, infinite, zeroSlope, point }
